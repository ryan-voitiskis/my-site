---
import { Image } from 'astro:assets'

interface Props {
	src: string
	alt: string
}

const { src, alt } = Astro.props

const imageFile = await import(`../assets/posts/${src}`)
---

<Image src={imageFile.default} alt={alt} format="webp" />
