---
import ContactForm from '@/components/ContactForm.vue'
import IconChevronsDown from '@/components/icons/IconChevronsDown.vue'
import { Button } from '@/components/ui/button'
import Layout from '@/layouts/Layout.astro'
import type { HeadItems } from 'astro-helmet'

const description = 'developer from melbourne, au. fullstack, mostly with js.'
const title = 'ryan_voitiskis'
const url = 'https://ryanvoitiskis.com'
const headItems: HeadItems = {
	title: title,
	meta: [
		{ name: 'description', content: description },
		{ property: 'og:title', content: title },
		{ property: 'og:description', content: description },
		{ property: 'og:url', content: url },
		{ property: 'twitter:card', content: 'summary' },
		{ property: 'twitter:title', content: title },
		{ property: 'twitter:description', content: description }
	],
	link: [{ rel: 'canonical', href: url }]
}
---

<Layout {headItems} navItem="about">
	<main class="mx-auto w-full px-4 pb-48">
		<div class="mx-auto w-full max-w-[1120px] px-4">
			<h1 class="text-muted-foreground font-mono text-sm font-[120]">
				> ryan_voitiskis
			</h1>
		</div>
		<div
			class="mx-auto flex w-full flex-col items-center justify-center gap-8 pb-48 md:flex-row"
		>
			<div
				class="text-muted-foreground w-full max-w-64 pt-16 text-sm md:self-start"
			>
				<p class="mb-2">developer from melbourne, au</p>
				<p class="mb-2">fullstack, mostly with js</p>
				<Button
					id={'talk_to_me'}
					variant="blank"
					class="pl-0 font-sans font-medium"
					aria-label="focus on name input field"
				>
					talk to me
					<IconChevronsDown class="mt-[1px] ml-1 h-5 w-5 md:-rotate-90" />
				</Button>
			</div>
			<ContactForm class="w-full max-w-[475px]" client:only="vue" />
		</div>
	</main>
</Layout>

<script>
	document
		.getElementById('talk_to_me')
		?.addEventListener('click', () =>
			(document.getElementsByClassName('name_input')[0] as HTMLElement).focus()
		)
</script>
